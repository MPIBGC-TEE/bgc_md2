# this requirement file is conda specific
# it only lists those packages that are not extractable from setup.py
# which are extracted by the installer automatically
# in this case we only need pip (to call pip install -e for the source package)
pip
